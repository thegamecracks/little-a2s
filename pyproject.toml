[build-system]
requires = ["uv-build~=0.9.7"]
build-backend = "uv_build"

[project]
name = "little-a2s"
version = "0.1.0"
description = "A sans-I/O library implementing the A2S Valve Source Query protocol."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "thegamecracks" }]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: SQL",
    "Topic :: Internet",
    "Topic :: System :: Networking",
    "Typing :: Typed",
]
dependencies = []

[dependency-groups]
dev = [
    "coverage~=7.11",
    "pytest~=8.4",
]

[project.urls]
# https://packaging.python.org/en/latest/specifications/well-known-project-urls/#well-known-labels
Homepage = "https://github.com/thegamecracks/little-a2s"
GitHub = "https://github.com/thegamecracks/little-a2s"
Issues = "https://github.com/thegamecracks/little-a2s/issues"

[tool.pytest.ini_options]
addopts = ["--import-mode=importlib"]
